

function calculation(number1, number2) {
    const squared1 = number1 * number1;
    const squared2 = number2 * number2;
    const total = squared1 + squared2;
    const sum = total * total;

    return sum;
}
console.log(calculation(2, 2))

const multiplie = function (number1, number2) {
    const squared1 = number1 * number1;
    const squared2 = number2 * number2;
    const total = squared1 + squared2;
    const sum = total * total;

    return sum;
};

console.log(multiplie(2, 2))


const squaring = (number1, number2) => {
    const squared1 = number1 * number1;
    const squared2 = number2 * number2;
    const total = squared1 + squared2;
    const sum = total * total;

    return sum;
};

console.log(squaring(2, 2))
